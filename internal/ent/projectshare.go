// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Fishwaldo/mcp-docstore/internal/ent/project"
	"github.com/Fishwaldo/mcp-docstore/internal/ent/projectshare"
	"github.com/Fishwaldo/mcp-docstore/internal/ent/user"
	"github.com/google/uuid"
)

// ProjectShare is the model entity for the ProjectShare schema.
type ProjectShare struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Permission holds the value of the "permission" field.
	Permission projectshare.Permission `json:"permission,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProjectShareQuery when eager-loading is set.
	Edges                    ProjectShareEdges `json:"edges"`
	project_shares           *uuid.UUID
	project_share_user       *uuid.UUID
	project_share_created_by *uuid.UUID
	selectValues             sql.SelectValues
}

// ProjectShareEdges holds the relations/edges for other nodes in the graph.
type ProjectShareEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// CreatedBy holds the value of the created_by edge.
	CreatedBy *User `json:"created_by,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectShareEdges) ProjectOrErr() (*Project, error) {
	if e.Project != nil {
		return e.Project, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: project.Label}
	}
	return nil, &NotLoadedError{edge: "project"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectShareEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// CreatedByOrErr returns the CreatedBy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectShareEdges) CreatedByOrErr() (*User, error) {
	if e.CreatedBy != nil {
		return e.CreatedBy, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "created_by"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProjectShare) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case projectshare.FieldPermission:
			values[i] = new(sql.NullString)
		case projectshare.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case projectshare.FieldID:
			values[i] = new(uuid.UUID)
		case projectshare.ForeignKeys[0]: // project_shares
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case projectshare.ForeignKeys[1]: // project_share_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case projectshare.ForeignKeys[2]: // project_share_created_by
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProjectShare fields.
func (_m *ProjectShare) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case projectshare.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case projectshare.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case projectshare.FieldPermission:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission", values[i])
			} else if value.Valid {
				_m.Permission = projectshare.Permission(value.String)
			}
		case projectshare.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field project_shares", values[i])
			} else if value.Valid {
				_m.project_shares = new(uuid.UUID)
				*_m.project_shares = *value.S.(*uuid.UUID)
			}
		case projectshare.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field project_share_user", values[i])
			} else if value.Valid {
				_m.project_share_user = new(uuid.UUID)
				*_m.project_share_user = *value.S.(*uuid.UUID)
			}
		case projectshare.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field project_share_created_by", values[i])
			} else if value.Valid {
				_m.project_share_created_by = new(uuid.UUID)
				*_m.project_share_created_by = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProjectShare.
// This includes values selected through modifiers, order, etc.
func (_m *ProjectShare) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProject queries the "project" edge of the ProjectShare entity.
func (_m *ProjectShare) QueryProject() *ProjectQuery {
	return NewProjectShareClient(_m.config).QueryProject(_m)
}

// QueryUser queries the "user" edge of the ProjectShare entity.
func (_m *ProjectShare) QueryUser() *UserQuery {
	return NewProjectShareClient(_m.config).QueryUser(_m)
}

// QueryCreatedBy queries the "created_by" edge of the ProjectShare entity.
func (_m *ProjectShare) QueryCreatedBy() *UserQuery {
	return NewProjectShareClient(_m.config).QueryCreatedBy(_m)
}

// Update returns a builder for updating this ProjectShare.
// Note that you need to call ProjectShare.Unwrap() before calling this method if this ProjectShare
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProjectShare) Update() *ProjectShareUpdateOne {
	return NewProjectShareClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProjectShare entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProjectShare) Unwrap() *ProjectShare {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProjectShare is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProjectShare) String() string {
	var builder strings.Builder
	builder.WriteString("ProjectShare(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("permission=")
	builder.WriteString(fmt.Sprintf("%v", _m.Permission))
	builder.WriteByte(')')
	return builder.String()
}

// ProjectShares is a parsable slice of ProjectShare.
type ProjectShares []*ProjectShare
